{
  "name": "the-middlemen",
  "version": "1.0.0",
  "description": "eBay watch engine — find rare items before anyone else",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm start",
    "setup": "cp backend/.env.example backend/.env && echo '\\n✓ Copied .env.example to backend/.env\\n  → Edit backend/.env and add your eBay API keys'"
  }
}
